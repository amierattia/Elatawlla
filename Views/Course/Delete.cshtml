```cshtml
@model NetWork.Models.Course
@{
    ViewData["Title"] = "حذف الدورة";
    Layout = "_Layout";
}

<div class="container mx-auto px-4 py-8 animate-fade-in" dir="rtl">
    <h1 class="text-3xl font-bold text-red-600 mb-6">حذف الدورة</h1>
    <div class="bg-white rounded-lg shadow-md p-6">
        <div class="bg-yellow-100 text-yellow-800 border border-yellow-300 rounded-md p-4 mb-6">
            <p>هل أنت متأكد من رغبتك في حذف هذه الدورة؟</p>
        </div>

        <dl class="grid grid-cols-1 sm:grid-cols-3 gap-4 mb-6">
            <dt class="text-gray-700 font-medium">اسم الدورة:</dt>
            <dd class="text-gray-700 sm:col-span-2">@Model.Title</dd>
            <dt class="text-gray-700 font-medium">الوصف:</dt>
            <dd class="text-gray-700 sm:col-span-2">@Model.Description</dd>
        </dl>

        <form asp-action="DeleteConfirmed" method="post">
            <input type="hidden" asp-for="Id" />
            <div class="flex space-x-4 space-x-reverse">
                <button type="submit" class="btn-hover-effect bg-red-600 text-white font-semibold py-2 px-6 rounded-md hover:bg-red-700 transition duration-200">
                    حذف
                </button>
                <a asp-action="Index" asp-controller="Course" class="bg-gray-500 text-white font-semibold py-2 px-6 rounded-md hover:bg-gray-600 transition duration-200">
                    إلغاء
                </a>
            </div>
        </form>
    </div>
</div>